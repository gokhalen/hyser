1) Change the 2D and 3D Gaussian quadrature library so that it is calculated as a tensor product of the 1D library quadpoints and weights. No use in redefining it. 

2) Arbitrary order uniform quadrature.
